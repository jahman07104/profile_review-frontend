import React from 'react';

const comments = () => {
return (
  <div>comments here
   {/* renderFieldLength() {
    <p>{`${this.state.field.length} character(s).`}</p>
    
    }
  
     validateFieldLength() {
      if (this.state.submitDisabled && this.state.field.length >     100) {
        this.setState({ submitDisabled: false });
      } else if (this.state.submitDisabled &&      this.state.field.length <= 100) {
        this.setState({ submitDisabled: true });
      }
     } 
  
  
     updateFieldLength(event) {
      const field = event.target.value;
      this.setState({ field }, () => {
        this.validateFieldLength();
      });
    }
  
     submitForm() {
      alert("Submitting the comments.");
   } */}
  </div>
)

}

  export default comments